# Firebase Movie Review Application

A browser-based CRUD application for adding, sorting, editing, and deleting movie reviews.

## Technologies
HTML, CSS, JavaScript, Firebase, and Cloud Firestore.

Before running the app, replace the placeholder Firebase configuration in `app.js` with a valid project configuration. Do not commit private credentials.
